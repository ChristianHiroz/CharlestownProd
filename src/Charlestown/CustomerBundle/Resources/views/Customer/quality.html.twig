{% extends 'CharlestownCustomerBundle::layout.html.twig' %}
{% block content %}
{{ parent() }}
<style> {# TODO : put in main.css rule only apply here sadly #}
    div .in{
        background-color: white !important;
    }
</style>
<div class="container charlestown-container">
    <div class="row charlestown-row">
        <div class="col-md-12 charlestown-col-md-12">
            <div class="charlestown-title">
                <h2>Compte rendu qualité</h2>
            </div>
            <nav class="navbar navbar-default secondNav">
                <div class="container-fluid">
                    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
                        <ul class="nav navbar-nav" style="width:100%;">
                            <li role="presentation" class="active">
                                <a href="#buy" id="buy-tab" role="tab" data-toggle="tab" aria-controls="buy" aria-expanded="true">Contrôles contradictoire</a>
                            </li>
                            <li class="divider-vertical-second"></li>
                            <li class="" role="presentation">
                                <a aria-expanded="false" href="#trade" role="tab" id="trade-tab" data-toggle="tab" aria-controls="trade">Contrôles site</a>
                            </li>
                            <li class="divider-vertical-second"></li>
                            <li class="" role="presentation">
                                <a aria-expanded="false" href="#gift" role="tab" id="gift-tab" data-toggle="tab" aria-controls="gift">Appels mystères</a>
                            </li>
                            <li class="divider-vertical-second"></li>
                            <li class="" role="presentation">
                                <a aria-expanded="false" href="#gifte" role="tab" id="gifte-tab" data-toggle="tab" aria-controls="gifte">Visites mystères</a>
                            </li>
                            <li class="divider-vertical-second"></li>
                            <li class="" role="presentation">
                                <a aria-expanded="false" href="#lookingfor" role="tab" id="lookingfor-tab" data-toggle="tab" aria-controls="lookingfor">Rapports d'activité</a>
                            </li>
                            <li class="divider-vertical-second"></li>
                            <li class="" role="presentation">
                                <a aria-expanded="false" href="#announce" role="tab" id="announce-tab" data-toggle="tab" aria-controls="announce">Enquêtes satisfaction</a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
            <div id="myTabContent" class="tab-content charlestown-scrollable">
                <div role="tabpanel" class="tab-pane fade active in" id="buy" aria-labelledby="buy-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.contradictoryControls %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="trade" aria-labelledby="trade-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.siteControls %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="gift" aria-labelledby="gift-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.mysteriousCalls %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="gifte" aria-labelledby="gifte-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.mysteriousVisits %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="lookingfor" aria-labelledby="lookingfor-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.activityReports %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
                <div role="tabpanel" class="tab-pane fade" id="announce" aria-labelledby="announce-tab">
                    <div class="container">
                        <div class="row">
                            {% set empty = false %}
                            {% for bill in user.investigations %}
                            {% set empty = true %}
                            {% if loop.index % 6 == 0 %}
                        </div>
                        <div style="margin-top: 15px;" class="row">
                            {% endif %}
                            <div class="col-md-2 textAlignCenter">
                                <a style="color:white; font-size: 20px;" download="{{ asset(bill.uploadRootDir  ~ bill.id ~ '.' ~ bill.alt) }}" href="{{ asset(bill.uploadRootDir  ~bill.id ~ '.' ~ bill.alt) }}">
                                    <span style="color:red;" class="glyphicon glyphicon-file"></span><br/>
                                    {{ bill.nameShow }}
                                </a>
                            </div>
                            {% endfor %}
                            {% if empty == 0 %}
                                <div>
                                    <p>Vous n'avez aucune factures, veuillez contacter votre responsable client pour plus d'informations.</p>
                                </div>
                            {% endif %}
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
