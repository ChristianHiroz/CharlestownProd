{% extends 'CharlestownCollaboratorBundle::layout.html.twig' %}
{% block content %}
    {{ parent() }}
    <div class="container" style="width:80%; margin-left:20%; max-width:80%;">
        <div class="row">
            <div class="col-md-12">
                <h2 class="bigTitle" style="text-align: right;">MON COURS</h2>
                <hr>
                {{ form(delete_form) }}
                <div class="form">
                    {{ form_start(edit_form)}}
                    {{ form_widget(edit_form.type, {'attr' : {'class' : 'champ', 'placeholder' : 'Nom'}}) }}
                    {{ form_widget(edit_form.description, {'attr' : {'class' : 'champ', 'placeholder' : 'Description'}}) }}
                    {{ form_label(edit_form.startAt) }}
                    {{ form_widget(edit_form.startAt, {'attr' : {'class' : 'champ', 'placeholder' : 'Début'}}) }}
                    {{ form_label(edit_form.endAt) }}
                    {{ form_widget(edit_form.endAt, {'attr' : {'class' : 'champ', 'placeholder' : 'Fin'}}) }}
                    {{ form_label(edit_form.room) }}
                    {{ form_widget(edit_form.room, {'attr' : {'class' : 'champ', 'placeholder' : 'Places'}}) }}
                    {{ form_widget(edit_form.localisation, {'attr' : {'class' : 'champ', 'placeholder' : 'Lieu'}}) }}
                    {{ form_widget(edit_form.address, {'attr' : {'class' : 'champ', 'placeholder' : 'Adresse'}}) }}
                    {{ form_widget(edit_form.town, {'attr' : {'class' : 'champ', 'placeholder' : 'Ville'}}) }}
                    {{ form_widget(edit_form.pc, {'attr' : {'class' : 'champ', 'placeholder' : 'Code postal'}}) }}
                    <div>
                        <input class="bouton" style="margin-bottom:150px;"type="submit" value="Envoyer" />
                    </div>
                    {{ form_end(edit_form) }}
                </div>
            </div>
        </div>

    </div>
{% endblock %}